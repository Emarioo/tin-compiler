// import "tests/test_base.tin"

fun p() {
    // v++;
    // global v: int = 2;
    // printi(v);
}

fun main() {
    // p();
    // p();
    // p();

    // arr: char = 'h';

    // arr: char* = "Hello world";

    // printc(arr[3]);

    // test_array();
}